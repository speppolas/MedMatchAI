# Configurazione del Database PostgreSQL
DATABASE_URL=postgresql://username:password@localhost/medmatchint

# Chiave segreta per Flask (genera una chiave sicura e casuale)
FLASK_SECRET_KEY=genera_una_chiave_segreta_sicura_e_casuale

# Ambiente Flask (development, production, testing)
FLASK_ENV=production

# Impostazioni opzionali
# Timeout per l'eliminazione automatica dei PDF (in minuti, default: 30)
PDF_AUTO_DELETE_TIMEOUT=30

# Directory per i file caricati (default: uploads)
UPLOAD_FOLDER=uploads

# Configurazione del sistema ibrido PostgreSQL + LLM
# Percorso alla directory principale di llama.cpp
LLAMA_CPP_PATH=/percorso/completo/a/llama.cpp

# Percorso al file del modello (formato GGUF) - esempi:
# Mistral 7B Instruct (consigliato per uso medico)
LLM_MODEL_PATH=/percorso/completo/a/llama.cpp/models/mistral-7b-instruct-v0.2.Q4_K_M.gguf
# LLaMA 2 7B 
# LLM_MODEL_PATH=/percorso/completo/a/llama.cpp/models/llama-2-7b-chat.Q4_K_M.gguf

# Parametri opzionali LLM
# LLM_CONTEXT_SIZE=4096      # Dimensione del contesto (default: 4096)
# LLM_TEMPERATURE=0.7        # Temperatura (default: 0.7, più basso = più deterministico)